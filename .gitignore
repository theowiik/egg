result
result-*
.direnv/
.DS_Store

# Optional in-checkout settings, selected with an absolute EGG_CONFIG path.
/local.nix

# One personal config per file; the shared example stays tracked.
/configs/*.nix
!/configs/example.nix
