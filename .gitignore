result
result-*
.direnv/
.DS_Store

# Optional in-checkout settings, selected with an absolute EGG_CONFIG path.
/local.nix

# New user configs stay local; the template and owner machine configs are tracked.
/configs/*.nix
!/configs/example.nix
!/configs/puter.nix
!/configs/neo.nix
